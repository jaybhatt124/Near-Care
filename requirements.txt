flask>=3.0
python-dotenv
requests
pymongo[srv]     # MongoDB driver  (replaces mysql-connector-python)
certifi          # fixes SSL/TLS handshake errors connecting to MongoDB Atlas on Windows
# mysql-connector-python  # no longer needed
